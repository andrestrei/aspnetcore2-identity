@model IEnumerable<Domain.Identity.IdentityUserRole<int>>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FirstOrDefault().IdentityUserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstOrDefault().IdentityRoleId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdentityUser.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdentityRole.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdentityUserRoleId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdentityUserRoleId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdentityUserRoleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
